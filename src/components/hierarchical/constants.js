// Minimum height per page in pixels
export const MIN_PAGE_HEIGHT_PX = 28;

// Minimum column width in pixels
export const MIN_COLUMN_WIDTH = 250;

// Scale width on the left (for row number axis)
export const SCALE_WIDTH = 70;

// Short encoding names for display
export const SHORT_ENCODING_NAMES = {
  PLAIN: 'PLAIN',
  PLAIN_DICTIONARY: 'DICT',
  RLE: 'RLE',
  BIT_PACKED: 'BIT',
  DELTA_BINARY_PACKED: 'DELTA',
  DELTA_LENGTH_BYTE_ARRAY: 'DELTA_LEN',
  DELTA_BYTE_ARRAY: 'DELTA_BA',
  RLE_DICTIONARY: 'DICT',
  BYTE_STREAM_SPLIT: 'SPLIT',
};
